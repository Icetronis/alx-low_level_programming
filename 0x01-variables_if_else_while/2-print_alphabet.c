#include<stdio.h>
/**
*main - prints the letter of the alphabets in lower case
*it is done while returning zero to main.
*Reaturn: Always 0.
*/
int main(void)
{
char ch = 'a'
while (ch <= 'z');
{
putchar(ch);
++ch,
}
putchar('\n');
return (0);
}
